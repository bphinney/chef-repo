# Config file for vertx logs
filebeat:
  prospectors:
    -
      paths:
        - /opt/vertx/logs/*.log
      encoding: utf-8
      input_type: log
      fields_under_root: true
      fields:
        environment: "<%= node['chef_environment'] %>"
        application: "vertx"
      tail_files: true
