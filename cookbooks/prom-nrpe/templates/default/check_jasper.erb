#!/bin/bash
#!/bin/bash
check_jasper=`service jasper status | grep "Jasper Reports is running"`
if [ "$check_jasper" == "" ]; then
  echo "WARNING - Jasper is not running"; exit 1
else
  echo "OK - Jasper is running"; exit 0
fi

